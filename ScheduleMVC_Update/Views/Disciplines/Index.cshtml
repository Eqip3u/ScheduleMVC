@model IEnumerable<ScheduleMVC_Update.Models.DisciplineSet>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TitleDiscipline)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TitleDiscipline)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DisciplineId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DisciplineId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DisciplineId })
        </td>
    </tr>
}

</table>
